version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    python: mambaforge-latest
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
sphinx:
   configuration: docs/conf.py

conda:
  environment: docs/environment.yaml

formats:
  - pdf
  - epub
